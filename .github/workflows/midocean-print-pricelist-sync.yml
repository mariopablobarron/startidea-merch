name: MidOcean print pricelist sync

# Semanal lunes 02:00 UTC. Sincroniza tarifas de marcaje MidOcean.
# Cambian poco — semanal es suficiente.

# El disparo lo hace el crontab del VPS (merch-cron-runner.sh, 30 4 hora Madrid
# = 02:30 UTC, diario), más robusto (guardas de arranque/carga/docker + Telegram
# solo en fallo real). Se retiró el schedule de GitHub Actions para no duplicar
# ni chocar con el crontab (409 del cron-lock). Queda workflow_dispatch a mano.
on:
  workflow_dispatch: {}

concurrency:
  group: midocean-print-pricelist-sync
  cancel-in-progress: false

jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - name: Disparar midocean-print-pricelist-sync
        uses: ./.github/actions/cron-trigger
        with:
          name: midocean-print-pricelist-sync
          url: https://merchandising.startidea.es/api/cron/midocean-print-pricelist-sync
          secret: ${{ secrets.MERCH_CRON_SECRET }}
          accept: "200 202"
